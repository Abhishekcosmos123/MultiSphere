import ProfileSettings from "./profile-settings-page"
import type { ProfileSettingsProps } from "./profile-settings-page"

export const ProfileSettingsComponent = (props: ProfileSettingsProps) => {
  return <ProfileSettings {...props} />
}
export { ProfileSettings }

